interface:
  display_name: "Programmable V4 Hook Builder"
  short_description: "Build and launch exact projects through Programmable V3"
  default_prompt: "Use $programmable-v4-hook-builder and the public Programmable V3 discovery, exact CLI, schema, remote preflight, separate wallet handoff, and status lifecycle. Never use the retired GitHub intake, expose a key, sign, or broadcast."
policy:
  allow_implicit_invocation: true
